# Server
PORT=5000
NODE_ENV=development

# MongoDB (MongoDB Atlas connection string in production)
MONGODB_URI=mongodb://127.0.0.1:27017/ok_strojevi

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# CORS — comma-separated list of allowed origins (your Netlify URL in prod)
CLIENT_ORIGIN=http://localhost:5173
